import Cards from "../../data/arrivalCards.json";

const state = {
  data: Cards.data,
};

// getters
const getters = {};

// mutations
const mutations = {};

// actions
const actions = {};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
